---
export const prerender = false;
import AuthShell from '../../layouts/AuthShell.astro';
import { RecuperarForm } from '../../components/editor/AuthForms';
import auth from '../../data/auth.json';
---

<AuthShell titulo={auth.recuperar.titulo} subtitulo={auth.recuperar.subtitulo}>
  <RecuperarForm client:load />
</AuthShell>
