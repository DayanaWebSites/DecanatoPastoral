---
export const prerender = false;
const rest = Astro.params.path || '';
if (rest === 'historial' || rest === 'usuarios') return Astro.redirect(`/editar/${rest}`);
const dest = rest.startsWith('/') ? rest : `/${rest}`;
return Astro.rewrite(dest);
---
