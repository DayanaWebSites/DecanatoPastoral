# /sigma-impact

Blast radius con GitNexus antes de tocar un módulo.

1. `impact({ target: "<símbolo o archivo>", direction: "upstream" })`
2. Reportar callers, procesos, risk level
3. Si HIGH/CRITICAL: no editar hasta confirmación

God nodes: `textos.json`, `@theme`, fotos de comedores, aviso de privacidad, `_source/` en gitignore.
