# A0 · Pre-flight

1. Lee `_shared/00-pre-flight.md`, `_shared/01-guardrails.md` y `_shared/08-gates-zentinek.md`.
2. `npm ci && npm run build` → 0, 23 páginas.
3. `npm run verify` → auditoría del SG-01 en verde.
4. `git checkout -b shot/sb-decanato-02-deploy`.
5. Confirma que existen `Dockerfile`, `nginx.conf` y `.dockerignore` en la raíz.

## Cierre
Los cinco puntos, con la salida de `npm run verify` pegada en el reporte del bloque.
